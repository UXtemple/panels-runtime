import assert from 'assert';
import Panels from '../panels';

describe('Panels', () => {
  xit('subscribes to the lists of panels from the router');
  xit('renders a PanelsUi component');
  xit('renders a PanelContainer component for each panel on the router\'s list inside the PanelsUi component');
});
