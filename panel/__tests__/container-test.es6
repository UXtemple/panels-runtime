import assert from 'assert';
import PanelContainer from '../container';

describe('PanelContainer', () => {
  xit('subscribes to the panel from store');
  xit('renders a Panel component');

  describe('expected props', () => {
    xit('context');
    xit('panel');
    xit('uri');
  });
});
